var page
var buf

after:
buf = kmalloc(_)
buf = kzalloc(_)
do:
test buf
then:

kfree(buf)

// kmap and kunmap are deprecated
buf = kmap(page)
buf = kmap_atomic(page)
buf = kmap_local_page(page)
buf = kmap_local_folio(page)

kunmap(buf)
kunmap_atomic(buf)
kunmap_local(buf)
kunmap_local_atomic(buf)

