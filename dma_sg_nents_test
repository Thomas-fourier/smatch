var dev
var sg
var nents
var dma_nents
var dir
var attrs


/////////////////////////////////////////
// DMA SG interface

// Mapping funcitons
dma_nents = dma_map_sg_attrs(dev, sg, nents, dir, attrs)
dma_nents = ib_dma_map_sg_attrs(dev, sg, nents, dir, attrs)
dma_nents = ib_dma_map_sg(dev, sg, nents, dir)

// Unmapping
dma_unmap_sg_attrs(dev, sg, nents, dir, attrs)
ib_dma_unmap_sg_attrs(dev, sg, nents, dir, attrs)
ib_dma_unmap_sg(dev, sg, nents, dir)

// Syncing 
dma_sync_sg_for_device(dev, sg, nents, dir)
dma_sync_sg_for_cpu(dev, sg, nents, dir)

sg_copy_buffer(sg, nents)
sg_copy_from_buffer(sg, nents)
sg_copy_to_buffer(sg, nents)
sg_pcopy_from_buffer(sg, nents)
sg_pcopy_to_buffer(sg, nents)



/////////////////////////////////
// DMA map api

var dma_add
var buf
var size

dma_mapping_error(dev, dma_add)
enic_dma_map_check(_, dma_add)
iwl_txq_gen2_set_tb_with_wa(_,_,_,dma_add)
ib_dma_mapping_error(dev, dma_add)
fc_dma_mapping_error(dev, dma_add)
vring_mapping_error(dev, dma_add)
virtqueue_dma_mapping_error(dev, dma_add)
b43legacy_dma_mapping_error(_, dma_add, size)
b43_dma_mapping_error(_, dma_add, size)


after:
dma_add = dma_map_single_attrs(dev, buf, size, dir, attrs)
dma_add = dma_map_page_attrs(dev, buf, size, dir, attrs)
dma_add = dma_map_resource(dev, buf, size, dir, attrs)
dma_add = __skb_frag_dma_map(dev, buf, size, dir, attrs)
dma_add = owl_emac_dma_map_rx(_)
dma_add = owl_emac_dma_map_tx(_)

do:
test dma_add with dma_mapping_error or enic_dma_map_check or iwl_txq_gen2_set_tb_with_wa

after:
dma_add = ib_dma_map_single(dev, buf, size, dir, attrs)
dma_add = ib_dma_map_page(dev, buf, size, dir, attrs)
do:
test dma_add with ib_dma_mapping_error

after:
dma_add = fc_dma_map_single(dev, buf, size, dir)
do:
test dma_add with fc_dma_mapping_error

after:
dma_add = vring_map_single(dev, buf, size, dir)
do:
test dma_add with vring_mapping_error

after:
dma_add = virtqueue_dma_map_single_attrs(dev, buf, size, dir, attrs)
do:
test dma_add with virtqueue_dma_mapping_error

after:
dma_add = map_descbuffer(_, buf, size, _)
do:
test dma_add with b43legacy_dma_mapping_error or b43_dma_mapping_error


ignore:
dma_map_area

then:

////////////////////////////////////
// Malloc and free
var page
after:
buf = kmalloc(_)
buf = kzalloc(_)
do:
test buf
then:

kfree(buf)

buf = kmap(page)
buf = kmap_atomic(page)
kunmap(page)
kunmap_atomic(page)

